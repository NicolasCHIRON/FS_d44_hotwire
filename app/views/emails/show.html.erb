<%= turbo_frame_tag "show_email" do %>
  <h1><%= @email.object %></h1></br>
  <p><%= @email.body %></p>
  <%= button_to "Supprimer", @email, method: :delete, data: {turbo_frame: "new_email"} %> <%= button_to "Marquer comme non lu" %>
<% end %>